# Development environment variables

# App URLs
BETTER_AUTH_URL=http://localhost:5173
VITE_PUBLIC_API_URL=http://localhost:5173
VITE_PUBLIC_ZERO_URL=http://localhost:8080

# Auth/session
BETTER_AUTH_SECRET=dev-secret-change-in-prod

# Redis
# If using docker-compose (maps container 6379 -> host 50001), use 50001:
# REDIS_URL=redis://localhost:50001
# If you have a local Redis on default port, use:
REDIS_URL=redis://localhost:6379

# Database (use Postgres from docker-compose)
ZERO_UPSTREAM_DB=postgres://user:password@localhost:50000/postgres
# If você quiser SQLite local (incompleto p/ este schema), use:
# ZERO_UPSTREAM_DB=./local.db

# Required placeholders for runtime validation (safe dummy values for dev)
AI_GATEWAY_API_KEY=dev
EXA_API_KEY=dev
RESEND_API_KEY=dev
STRIPE_SECRET_KEY=sk_test_dev
STRIPE_WEBHOOK_SECRET=whsec_dev
PRO_MONTHLY_PRICE_ID=price_dev
CRON_SECRET=dev_cron
GOOGLE_CLIENT_ID=dev
GOOGLE_CLIENT_SECRET=dev
GITHUB_CLIENT_ID=dev
GITHUB_CLIENT_SECRET=dev
